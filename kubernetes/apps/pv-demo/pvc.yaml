# Том на LINSTOR: две реплики DRBD на разных воркерах (StorageClass linstor-r2).
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pv-demo
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: linstor-r2
  resources:
    requests:
      storage: 1Gi
