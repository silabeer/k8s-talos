apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# operator.yaml: kubectl kustomize "https://github.com/piraeusdatastore/piraeus-operator//config/default?ref=v2.11.0"
# Вендорим, чтобы Argo CD не ходил на GitHub за манифестами. Обновление: перерендерить с новым ref.
resources:
  - operator.yaml
  - linstor.yaml
