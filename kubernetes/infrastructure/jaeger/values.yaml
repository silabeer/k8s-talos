# Jaeger в варианте all-in-one: сборщик, хранилище и UI в одном поде.
# Хранилище в памяти, при перезапуске трассировки теряются — для стенда
# этого достаточно, а узлы здесь 2 ядра и 4 ГБ.
image: jaegertracing/all-in-one:1.76.0
# Сколько трассировок держать в памяти.
maxTraces: 20000
