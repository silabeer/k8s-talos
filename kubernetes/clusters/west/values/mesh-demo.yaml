cluster: west

# Сервер нужен в обоих кластерах: на нём показывается маршрутизация по
# заголовку в конкретный кластер. Свойство "сервис есть, подов нет"
# демонстрируют сервисы mesh-demo-<кластер>: в каждом кластере есть оба,
# а endpoint'ы — только у своего.
